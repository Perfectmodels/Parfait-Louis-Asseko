## 2024-03-06 - Initial setup
**Learning:** Development and production builds (`pnpm dev` and `pnpm build`) fail initially due to missing missing files (`PWAInstaller`, `Marquee`, `ModelDetail`) and incorrect config.
**Action:** The build failures out-of-the-box are expected per instructions, my optimization is strictly focused on ModelCard components.
